export default {
  title: 'Configure the prefered announcement channel.',
  baseCommand: '!config announcement-channel {#channel}',
  requiresAdmin: true,
  examples: ['!config announcement-channel here', '!config announcement-channel #general']
};
