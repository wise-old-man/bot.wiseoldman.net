export default {
  title: 'Configure the default preferred broadcast channel.',
  baseCommand: '!config channel:default {#channel}',
  requiresAdmin: true,
  examples: ['!config channel:default here', '!config channel:default #general']
};
