export default {
  title: "Configure the server's command prefix.",
  baseCommand: '!config prefix {prefix}',
  requiresAdmin: true,
  examples: ['!config prefix $', '!config prefix +', '!config prefix ++']
};
